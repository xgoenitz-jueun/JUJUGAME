{
  "name": "chibi-side-scroll-game",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:mobile": "node scripts/dev-mobile.mjs",
    "build": "tsc --noEmit && vite build",
    "test": "node --test tests/*.test.cjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "qrcode-terminal": "^0.12.0",
    "typescript": "5.9.3",
    "vite": "8.3.1"
  }
}

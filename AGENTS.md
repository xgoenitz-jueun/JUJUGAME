# Git 提交身分

在此專案建立任何提交之前，請在本機設定匿名作者與提交者：

```sh
git config --local user.name "JUJUGAME Contributors"
git config --local user.email "jujugame@users.noreply.github.com"
```

提交前檢查作者與提交者欄位；不可使用私人電子郵件地址。既有提交歷史已清除 Gmail 地址，請勿重新推回舊提交。
